# Legacy Reference

The legacy WinForms ZIP is reference-only and is not copied into this public source tree.

Original inputs are kept under `migration_inputs/` during local migration work and ignored by git. Do not commit extracted `bin/`, `obj/`, `.vs/`, vendor DLL, `.exe`, or `.pdb` artifacts.
